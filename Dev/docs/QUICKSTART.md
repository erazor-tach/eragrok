# ⚡ THRESHOLD C++ — Quick Start WSL

## Installation (une fois)
```bash
sudo apt update
sudo apt install -y build-essential cmake libsqlite3-dev
```

## Compilation
```bash
cd /mnt/e/Projet/GIT/C
chmod +x build.sh
./build.sh
```

## Exécution
```bash
./build/threshold_test
```

## Alternative rapide
```bash
make run  # Compile + exécute
```

---

📖 **Guide complet** : README_WSL.md
